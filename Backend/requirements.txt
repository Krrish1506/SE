# Core framework
fastapi==0.104.1
uvicorn==0.24.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Auth
PyJWT==2.9.0
passlib==1.7.4
bcrypt==4.1.1
cryptography==41.0.7

# Validation & schemas
pydantic==2.5.0
email-validator==2.1.0

# Utilities
python-multipart==0.0.6
python-dotenv==1.0.0

# NOTE: 'os' is a Python built-in standard library module.
# It does NOT need to be listed here — it is available in every Python environment automatically.
